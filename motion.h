#include "wifi.h"

void shoot(float angle,int bot_id,int socket);
void pass(int bot_id,int socket);
void movement(int bot_id, int socket, float bot_angle,float angle, float dist, float integral_error[5]);
